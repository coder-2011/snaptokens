2740e8dfd58910e7ca2d7e085da456a934a9cd2e
diff --git a/src/models/bpe.rs b/src/models/bpe.rs
index f0a6cfa6..592dcd15 100644
--- a/src/models/bpe.rs
+++ b/src/models/bpe.rs
@@ -1066,7 +1066,7 @@ const CACHE_SHARDS: usize = 64;
 const SHARED_CACHE_MAX_PER_SHARD: usize = 16 * 1024;
 
 struct SharedCache {
-    shards: Vec<Mutex<FxHashMap<String, Vec<u32>>>>,
+    shards: Vec<Mutex<FxHashMap<Box<str>, Vec<u32>>>>,
 }
 
 impl SharedCache {
@@ -1104,7 +1104,7 @@ impl SharedCache {
         if shard.len() >= SHARED_CACHE_MAX_PER_SHARD {
             shard.clear();
         }
-        shard.insert(key, value);
+        shard.insert(key.into_boxed_str(), value);
     }
 }
 
